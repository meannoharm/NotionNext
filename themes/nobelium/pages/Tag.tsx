import LayoutBase from '../layout/LayoutBase';
import TagList from '../components/TagList';

/**
 * 文章标签列表
 * @param {*} props
 * @returns
 */
const Tag = () => {
  return (
    <LayoutBase>
      <TagList />
    </LayoutBase>
  );
};

export default Tag;
